The following examples/folders needs additional build files:

- `web-worker`
- `geometry-optimization`
- `indexed-db`

Please run `npm run build-examples`, from the project's root, to generate these build files.
